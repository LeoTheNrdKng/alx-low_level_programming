#include <unistd.h>

/**
* _putchar - writes a program that prints _putchar, followed by a new line.
* print_alphabet - writes a function that prints the alphabet, in lowercase, followed by a new line.
* print_alphabet_x10 - print the alphabet ten  times
* _islower - return 1 if c is lowercase, 0 otherwise
*/

int _putchar(char);
void print_alphabet(void);
void print_alphabet_x10(void);
int _islower(int);
